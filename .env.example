# ENTSO-E Transparency Platform API key — used to fetch live Greek DAM prices.
# Without it the ingestion module falls back to a flagged synthetic series.
# Get a free key from https://transparency.entsoe.eu/
ENTSOE_API_KEY=
